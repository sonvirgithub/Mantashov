import React, { useState, useEffect, useContext } from "react";
import "../MobilePresenceCards/MobilePresenceCards.css";
import "./LargerDescriptionOfPresenceCard.css";
import { connect } from "react-redux";
import  "../DesktopPresenceCard/DesktopPresenceCard.css";

function LargerDescriptionOfPresenceCard() {
 
  return (
    <div>
      
    </div>
  );
}
const mapStateToProps = (state) => {
  return {
 
  };
};
const mapDispatchToProps = (dispatch) => {
  return {
   
  };
};
export default connect(
  mapStateToProps,
  mapDispatchToProps
)(LargerDescriptionOfPresenceCard);
